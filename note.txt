Parser : 
 - il faut le faire retourner les valeur en Json
 - Avec les données du parser il faut crée les struct rust dans l'api ainsi que les routes personalliser
 - Faire un watcher qui va ecouter les changement de route.yaml uniquement quand le serveur tourne.


Pour l'instant 

 Miblo init rust 
    - Crée l'api minimal /rust
                            route.yaml  
                            /src
                            cargo.toml
                            etc...
    - Join le dossier /rust et lis le fichier route.yaml et retourne les valeurs
    
 Miblo run 
    - va dans le /rust et fait cargo run pour lancer le serv Axum minimal





A Faire : 

   - Automatiser le menu pour ajout d'un language autre que Rust pour crée l'api dans generator
   - Faire retourner les valeur de parser en Json
   - configurer les Err dans le parser 
   - Sécurisé les fonctions
   - Création des ( Routes struc bdd ) en fonction des données de ROUTE.yaml
   - Ajout d'un system d'auth uniquement sur abonement 
   - Création du site web pour crée un compte avec abonement 
   - ajoute du watcher de route.yaml quand le programme est lancer avec miblo run rust