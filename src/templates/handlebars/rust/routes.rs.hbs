use axum::{Router, routing::{get, post}};
use crate::handlers::*;

pub fn create_router() -> Router {
    Router::new()
    {{#each routes}}
        .route("{{ this.path }}", {{ this.method }}({{ this.name }}))
    {{/each}}
}