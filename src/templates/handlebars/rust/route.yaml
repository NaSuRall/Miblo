# [ ROUTES ]
route:
  - method: get
    path: "/user/bonjour"
    model: "Users"

  - method: post
    path: "/users/create"
    model: "Users"

# [ DATABASES ]
database:
  - DB_CONNECTION: mysql
    DB_HOST: 100.120.71.76
    DB_PORT: 3306
    DB_DATABASE: miblo_test
    DB_USERNAME: askeet
    DB_PASSWORD: Romain2024!
# [ SERVER ]
server:
  - port: 4000
  - adress: 0.0.0.0
# [ CONFIGURATIONS ]
auth: true

# [ MODEL ]
models:
  - name: "Users"
    fields:
      - name: id
        type: Binary
      - name: email
        type: String
      - name: password
        type: String
      - name: first_name
        type: String
      - name: last_name
        type: String
      - name: phone
        type: String
      - name: pseudo
        type: String
  
  - name: "Survey"
    fields:
      - name: name
        type: string
      - name: last_name
        type: string

